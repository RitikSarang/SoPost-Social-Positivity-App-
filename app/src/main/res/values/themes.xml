<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SoPost" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">#939393</item>
        <item name="colorPrimaryVariant">@color/appBg</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/appBg</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Button" parent="Widget.AppCompat.Button">
        <item name="backgroundTint">@color/appBg</item>
        <item name="android:padding">15dp</item>
        <item name="textAllCaps">false</item>
    </style>

    <style name="ButtonWhite" parent="Widget.AppCompat.Button">
        <item name="backgroundTint">@color/white</item>
        <item name="android:padding">15dp</item>
        <item name="textAllCaps">false</item>
    </style>
</resources>